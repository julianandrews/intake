[package]
name = "intake-ai"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
anyhow = "1"
rust_decimal = "1.42.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", features = ["json"] }

[dev-dependencies]
toml = "0.8"
